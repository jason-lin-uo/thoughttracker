# Project-local npm config.
# Forces public npmjs.org so the project installs cleanly on any machine,
# even when the user's global ~/.npmrc points at a private/corporate registry.
registry=https://registry.npmjs.org/
audit=false
fund=false
